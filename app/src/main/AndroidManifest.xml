<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arcus.laboratorijskevjezbe_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="laboratorijske_vjezbe_android"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PersonalInfoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StudentInfoActivity" />
        <activity android:name=".SummaryActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Student" />
        <activity android:name=".Naslov"></activity>
    </application>

</manifest>